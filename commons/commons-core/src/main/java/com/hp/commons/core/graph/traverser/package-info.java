/**
 * TODO the over-abundance of interfaces in this package shows that there's bad design here.
 * there is an attempt to support different traversal strategies - but it doesn't really work.
 * REFACTOR REQUIRED.
 *
 * contains the actual graph traversal logic, taking in all the different bits and pieces and
 * making one class that uses the different components to traverse the graph.
 *
 */
package com.hp.commons.core.graph.traverser;