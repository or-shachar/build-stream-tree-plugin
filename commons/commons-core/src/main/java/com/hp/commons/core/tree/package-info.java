/**
 * provides a common tree API for data that should be stored in a tree shape in a controlled manner.
 * not suitable as an implementation of a collection, but suitable for tree widgets for example.
 *
 * TODO: another implementation of a tree that is "naive" - wraps elements in nodes and delegates to a node tree
 */
package com.hp.commons.core.tree;